import 'package:flutter/material.dart';

class AppTheme {
  static final primaryLight = const Color(0xFF80CBC4).withOpacity(0.55);
  static const primaryDark = Color(0xFF004D40);
  static const secondaryColor = Color.fromARGB(255, 20, 20, 20);
}
